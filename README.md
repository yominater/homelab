# My 'homelab'
Running on a free oracle server, using Tailscale to access. Eventually I would like to include a reverse proxy.

Immich, Gitea, and a static web page are all served.

Access the dashboard [here](http://homelab.dzo-armadillo.ts.net:8080/) (requires VPN).
