version: "3.9"

services:
        app:
                container_name: web-app
                image: web-app
                build:
                        context: .
                        dockerfile: Dockerfile
                restart: unless-stopped
                ports:
                        - "8080:8080"



